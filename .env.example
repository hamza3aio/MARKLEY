# Public — NEXT_PUBLIC_* are bundled into the browser JS (safe: anon key + URL only)
NEXT_PUBLIC_SUPABASE_URL=https://xyzcompany.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_example

# Server-only — Vercel env + .env.local, NEVER exposed to the browser
SUPABASE_SERVICE_ROLE_KEY=sb_secret_example
APP_URL=http://localhost:3000
CRON_SECRET=

# Optional (skipped gracefully when unset)
# RESEND_API_KEY=
# RESEND_FROM=Markley <onboarding@resend.dev>
# AI_PROVIDER=openai-compatible
# AI_API_URL=https://api.openai.com/v1
# AI_API_KEY=
# AI_MODEL=gpt-4o-mini
